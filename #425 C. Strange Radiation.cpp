//Author:CookiC
//#include"stdafx.h"
#include<iostream>
#include<iomanip>
#define maxn 100010
#define INF 1000010
//#pragma warning(disable : 4996)
using namespace std;

const int MR=1e6;

double x,v,t,minl=INF,minlx,minr=INF,minrx,n,s;

bool ok(double T){
	int L=T*s,R=y-T*s,minL;
	for(i=0;i<n;++i)
		if(t[i]==1){
			if(x[i]<=L){
				L=max(x[i],L);
				R=min()
			}
		}
		else{
			if((MR-x[i])/s<=T)
		}
}

int main(){
//	freopen("test.in","r",stdin);
//	freopen("test.out","w",stdout);
	ios::sync_with_stdio(false);
	
	cin>>n>>s;
	for(i=0;i<n;++i)
		cin>>x[i]>>v[i]>>t[i];
	double ans;
	t=n/s;
	if(l<r)
		if(minl<t/2||minr<t/2){
			ans=minl<minr?max(l/s,minr):max((y-r)/s,minl);
		else
			ans=max(max(l,y-r),y/2)/s;
	else
		if()
	return 0;
}

